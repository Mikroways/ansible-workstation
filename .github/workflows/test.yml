---
name: Tests
on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.0
      - name: yamllint
        run: uv run yamllint .
      - name: ansible-lint
        run: uv run ansible-lint .

  molecule:
    name: Molecule (${{ matrix.ansible }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ansible:
          - ansible11
          - ansible12
          - ansible13
        command:
          - test
        # ansible14: molecule-plugins/community.docker aún no soportan el
        # templating de ansible-core 2.21 y el test completo falla en la
        # fase create (no es un problema del role). Se corre solo el syntax
        # check hasta que el tooling se actualice; promover a test entonces.
        include:
          - ansible: ansible14
            command: syntax
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.0
      - name: Run molecule
        run: uv run --no-default-groups --group ${{ matrix.ansible }} molecule ${{ matrix.command }}
